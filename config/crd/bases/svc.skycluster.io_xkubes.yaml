---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: xkubes.svc.skycluster.io
spec:
  group: svc.skycluster.io
  names:
    kind: XKube
    listKind: XKubeList
    plural: xkubes
    singular: xkube
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: XKube is the Schema for the xkubes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of XKube
            properties:
              applicationId:
                description: |-
                  applicationId is a unique identifier for the setup/application.
                  Must match the one used by the provider.
                minLength: 1
                type: string
              nodeCidr:
                description: nodeCidr is an optional node CIDR (commonly used in GCP
                  manifests).
                pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}\/[0-9]{1,2}$
                type: string
              nodeGroups:
                description: nodeGroups defines one or more node groups for the cluster.
                items:
                  description: NodeGroup defines a group of worker nodes and optional
                    autoscaling.
                  properties:
                    autoScaling:
                      description: AutoScaling settings (optional).
                      properties:
                        maxSize:
                          description: Maximum number of nodes.
                          format: int32
                          minimum: 1
                          type: integer
                        minSize:
                          description: Minimum number of nodes.
                          format: int32
                          minimum: 0
                          type: integer
                      required:
                      - maxSize
                      - minSize
                      type: object
                    instanceTypes:
                      description: InstanceTypes lists instance flavor identifiers,
                        e.g. ["4vCPU-16GB"].
                      items:
                        type: string
                      minItems: 1
                      type: array
                    publicAccess:
                      description: publicAccess indicates whether nodes in this group
                        receive public access.
                      type: boolean
                  required:
                  - instanceTypes
                  - publicAccess
                  type: object
                type: array
              podCidr:
                description: |-
                  podCidr describes pod CIDRs. For AWS this may include separate public/private sub-blocks.
                  For GCP it may only include cidr.
                properties:
                  cidr:
                    description: CIDR block for pods.
                    pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}\/[0-9]{1,2}$
                    type: string
                  private:
                    description: 'Optional: private pods CIDR (AWS example)'
                    pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}\/[0-9]{1,2}$
                    type: string
                  public:
                    description: 'Optional: public pods CIDR (AWS example)'
                    pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}\/[0-9]{1,2}$
                    type: string
                required:
                - cidr
                type: object
              principal:
                description: principal identifies the principal used to perform control-plane
                  actions.
                properties:
                  id:
                    description: 'ID depends on platform: ARN for AWS, member for
                      GCP, principalId for Azure, etc.'
                    minLength: 1
                    type: string
                  type:
                    description: 'Type of principal: user | role | serviceAccount
                      | servicePrincipal | managedIdentity'
                    minLength: 1
                    type: string
                required:
                - id
                - type
                type: object
              providerRef:
                description: Provider reference must match the provider instance configuration.
                properties:
                  configName:
                    type: string
                  name:
                    type: string
                  platform:
                    type: string
                  region:
                    type: string
                  regionAlias:
                    type: string
                  type:
                    type: string
                  zone:
                    type: string
                type: object
              serviceCidr:
                description: |-
                  serviceCidr is the CIDR for services (non-overlapping with VPC/node CIDRs).
                  Optional (commonly used in AWS manifests).
                pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}\/[0-9]{1,2}$
                type: string
            required:
            - applicationId
            - providerRef
            type: object
          status:
            description: status defines the observed state of XKube
            properties:
              conditions:
                description: The status of each condition is one of True, False, or
                  Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
